import { DragDashboardDirective } from './drag-dashboard.directive';

describe('DragDashboardDirective', () => {
  it('should create an instance', () => {
    const directive = new DragDashboardDirective();
    expect(directive).toBeTruthy();
  });
});
